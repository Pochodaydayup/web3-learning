# 第 2 章练习题

## 概念题

1. 调用上下文里最重要的几个字段是什么？
2. `call` 和 `delegatecall` 在 storage 语义上有什么差异？
3. 为什么 `msg.sender` 不能总被简单理解成“用户地址”？
4. 理解 opcode 分类为什么对工程师也有帮助？

## 分析题

1. 为什么代理模式里 implementation 的代码会修改 proxy 的状态？
2. 某函数在 trace 里经过多个外部调用后回滚。请说明为什么“看调用跳转”比只看最终报错更重要。
3. 为什么说理解调用上下文能帮助你更好读安全事故复盘？

## 代码题

1. 写一个最小示例，对比 `call` 和 `delegatecall` 后状态落在哪个合约。
2. 用注释说明某段 trace 里每一跳的上下文变化。
3. 给一个代理最小例子写一页上下文说明。
