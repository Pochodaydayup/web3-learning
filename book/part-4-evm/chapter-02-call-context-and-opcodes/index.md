# 第 2 章：Opcode 与调用上下文

理解了 storage、memory 和 calldata 之后，下一步要补的是“谁在当前上下文里执行，执行中到底能看到什么”。这会直接影响你对 `msg.sender`、`msg.value`、外部调用、`delegatecall` 和 trace 的理解。

## 本章要解决什么

本章要解决的是“我知道 EVM 在执行，但对调用上下文和 opcode 层含义还是抽象的”。我们不会把你变成汇编工程师，但会把最值得工程师掌握的上下文概念讲清楚。

## 读完后你应该会什么

- 解释当前调用上下文包含哪些关键信息
- 理解普通 `call` 和 `delegatecall` 在上下文层面的差异
- 知道为什么 trace 和低级调用会让问题变得更透明
- 对 opcode 有一个工程上够用的认识框架

## 本章核心概念

- **msg.sender**：当前这层调用在 EVM 看来真正的直接调用者。
- **msg.value**：当前调用随交易一同带入的 ETH 数量。
- **调用上下文（Call Context）**：当前执行所继承的发送者、金额和状态视角组合。
- **Delegatecall**：在当前合约状态上下文里执行另一份代码的低级调用方式。

## 本章目录

1. [调用上下文怎样改变语义](./call-context)
2. [练习题](./exercises)
3. [项目实践](./practice)
4. [延伸阅读](./reading)

## 练习与实践入口

如果你后面想真正看懂重入、代理和低级调用问题，这一章是必须经过的中间层。

## 延伸阅读入口

这章和卷六的 `delegatecall` 风险、卷九的代理源码阅读会直接连起来。
